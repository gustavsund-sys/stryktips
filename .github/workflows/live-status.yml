name: Update live results

on:
  workflow_dispatch:
  schedule:
    # Skapa/uppdatera nedräkningen när den nya kupongen släpps på torsdagen.
    - cron: '17 6-11 * * 4'
    # Fyra oberoende starter per timme minskar risken att GitHubs försenade
    # schemaläggning lämnar luckor. Varje session uppdaterar var femte minut
    # i högst 10 min; nästa start tar sedan över fem minuter senare. Börja
    # redan på förmiddagen så även omgångar med ovanligt tidig avspark täcks.
    - cron: '2,17,32,47 8-23 * * 6'
    - cron: '2,17,32,47 0-1 * * 0'

permissions:
  contents: read

concurrency:
  group: stryktipset-live-status
  cancel-in-progress: false

jobs:
  update-live:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v5
      - name: Test live parser
        run: node --test scripts/live-status.test.mjs
      - name: Fetch and publish live status
        run: |
          attempts=1
          if [ "$SCHEDULE" = "2,17,32,47 8-23 * * 6" ] || [ "$SCHEDULE" = "2,17,32,47 0-1 * * 0" ]; then
            attempts=3
          fi
          successes=0
          for attempt in $(seq 1 "$attempts"); do
            if output="$(node scripts/update-live-status.mjs)"; then
              echo "$output"
              successes=$((successes + 1))
              poll="$(echo "$output" | tail -n 1 | jq -r '.pollRecommended // false')"
              if [ "$poll" != "true" ]; then
                echo "Omgången behöver ingen tät livebevakning ännu."
                break
              fi
            else
              echo "Liveförsök $attempt misslyckades; nästa försök görs om fem minuter."
            fi
            if [ "$attempt" -lt "$attempts" ]; then sleep 300; fi
          done
          if [ "$successes" -eq 0 ]; then
            echo "Samtliga liveförsök misslyckades."
            exit 1
          fi
        env:
          SCHEDULE: ${{ github.event.schedule }}
          FIREBASE_PROJECT_ID: stryktipset-46f61
          FIREBASE_SERVICE_ACCOUNT_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
